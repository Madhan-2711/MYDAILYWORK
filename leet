class Solution {
    int num  ;    
    public int myAtoi(String s) {
        int regex = "[123456789]"
        int sign = "-"
        if (s.contains("-")){
            sign = "-";
        }esle{
            sign = "+";
        }

        for (int i  =0; i<s.length(); i++){
            if(s.charAt(i).equals(0)){
                continue;
            }
            if(s.charAt(i).contains(regex)){
                num 
                num = int
            }
        }


    }
}
